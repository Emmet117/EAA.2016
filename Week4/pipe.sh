#!/bin/bash

#PS -EF Shows every process on the system.
#Grep mysql: grep searches the named input files for lines containing a match to the
#       given pattern.

#ps -ef | grep mysql Shows every process that relates to mysql
